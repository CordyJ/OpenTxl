% Prolog factbase specification of the SGI GL library interface 
% A. Ryman, IBM Centre for Advanced Studies, IBM Canada Ltd.

% Abridged to only a few routines for TXL distribution - JRC 15.7.93

function(addtopup).
parameter(addtopup,in,long,pup).
parameter(addtopup,in,String,str).
parameter(addtopup,in,long,arg).

function(arc).
parameter(arc,in,Coord,x).
parameter(arc,in,Coord,y).
parameter(arc,in,Coord,radius).
parameter(arc,in,Angle,startang).
parameter(arc,in,Angle,endang).

function(arcf).
parameter(arcf,in,Coord,x).
parameter(arcf,in,Coord,y).
parameter(arcf,in,Coord,radius).
parameter(arcf,in,Angle,startang).
parameter(arcf,in,Angle,endang).

function(cmov2).
parameter(cmov2,in,Coord,x).
parameter(cmov2,in,Coord,y).

function(cmov2i).
parameter(cmov2i,in,Icoord,x).
parameter(cmov2i,in,Icoord,y).

function(cmov2s).
parameter(cmov2s,in,Scoord,x).
parameter(cmov2s,in,Scoord,y).

function(color).
parameter(color,in,Colorindex,c).

function(getzbuffer).
returns(getzbuffer,long,flag).

function(glcompat).
parameter(glcompat,in,int,mode).
parameter(glcompat,in,int,value).

function(gsync).

function(pagecolor).
parameter(pagecolor,in,Colorindex,pcolor).

function(pclos).

function(pdr).
parameter(pdr,in,Coord,x).
parameter(pdr,in,Coord,y).
parameter(pdr,in,Coord,z).

function(pdri).
parameter(pdri,in,Icoord,x).
parameter(pdri,in,Icoord,y).
parameter(pdri,in,Icoord,z).

function(swapbuffers).

function(swaptmesh).

function(swinopen).
parameter(swinopen,in,long,parent).
returns(swinopen,long,gid).
fails(swinopen,"gid==-1L").

function(textcolor).
parameter(textcolor,in,Colorindex,tcolor).

function(textinit).

function(winposition).
parameter(winposition,in,long,x1).
parameter(winposition,in,long,x2).
parameter(winposition,in,long,y1).
parameter(winposition,in,long,y2).

function(winpush).

function(winset).
parameter(winset,in,long,gid).

function(wintitle).
parameter(wintitle,in,char*,name).

function(zbuffer).
parameter(zbuffer,in,Boolean,b).

function(zclear).
